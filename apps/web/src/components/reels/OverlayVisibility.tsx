"use client";

/*
 * Kept for compatibility with existing imports.
 * Hover proximity is handled globally by useOverlay so this
 * component never blocks reel dragging near the viewport edges.
 */
export default function OverlayVisibility() {
  return null;
}
