export interface NewsPublication {
  /** Name of the news publication (must match name in Google News Publisher Center) */
  name: string;
  /** 2- or 3-letter ISO 639 language code (e.g., 'en', 'es', 'fr') */
  language: string;
}
